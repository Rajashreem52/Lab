node{
    checkout scm
    stage('Build') {
       withMaven(maven: 'maven') {
                    sh 'mvn compile'
                }
    }
    stage('Test') {
       withMaven(maven: 'maven') {
                    sh 'mvn test'
                }
    }
}
